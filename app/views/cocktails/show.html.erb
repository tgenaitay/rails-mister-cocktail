<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @cocktail.name %>
</p>

<ul>
<% @cocktail.doses.each do |dose| %>
  <li><%=dose.ingredient.name %> | <%=dose.description %> | <%= link_to cocktail_dose_path(@cocktail, dose), method: :delete do %> <i class="fa fa-trash"></i> <% end %>
</li>
<% end %>
</ul>

<p>
  <strong>
  <%= link_to 'Add dose', new_cocktail_dose_path(@cocktail) %>
  </strong>
</p>



<%= link_to 'Edit', edit_cocktail_path(@cocktail) %> |
<%= link_to 'Back', cocktails_path %>
